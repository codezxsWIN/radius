# BR-005: approval

Hand-derived alpha reach under default: 0/4; see tests/fixtures/README.md for the path and weight arithmetic.

Expected outputs are frozen from the independent whole-edge oracle, with pairwise-difference Gini rather than the production rank formula.

## default

- credential-alpha: pairs=[]; absolute=0; universe=4; canonical=0; sensitivity=0; action=0; k=1 bounded=0.

## strict

- credential-alpha: pairs=[]; absolute=0; universe=4; canonical=0; sensitivity=0; action=0; k=1 bounded=0.

## session-theft-aware

- credential-alpha: pairs=[]; absolute=0; universe=4; canonical=0; sensitivity=0; action=0; k=1 bounded=0.
