# Test Report

## Expanded Identity Evidence: 2026-09-16

- Latest user request to improve the tool after its real-input test: implemented bounded matrix expansion, literal env substitution, unresolved identity observations, broader literal CloudFormation recognition and the Identities/evidence-needed workflow. No engine/provider or live access claim was added.
- Required final `tools/verify.py`: **301 passed, 1 skipped in 69.98 seconds**; **3229/3611 executable lines, 89.421213%**. The skip remains Windows file-symlink creation privileges, not a waived regression. Changed source files have no editor diagnostics.
- Matrix checks: four resolvable scalar combinations retain distinct principals and correct per-variant role reach; include/exclude entries merge against originals; include-only, typed exclusions, string versions and exact integer values are covered. Dynamic/nested/empty/excessive matrices do not admit a role path. Bounds are 64 variants per matrix, 256 expanded jobs and 512 identity observations per workflow; direct identity-limit rejection is tested. Literal environment shadowing never falls back through an unresolved override. Source marks accompany env/matrix substitutions.
- Unresolved observations retain context/name references but no unknown role value, credential value or inferred local-action semantics. Known sensitive matrix field names are masked (not a general confidentiality guarantee). Findings and SARIF results remain empty for unknown identity evidence. Markdown neutralizes added guidance/reference text; SARIF keeps evidence summaries/gaps as run metadata.
- Literal CloudFormation YAML/JSON tests reproduce the old missed conventional paths, then verify `.template/.cfn/.cloudformation` forms, `template.yaml/json` and a `cloudformation/` directory. Known intrinsic scalar/list/mapping tags remain opaque to every literal helper. Intrinsic RoleName cannot create a path; unrelated opaque metadata does not erase independently literal evidence. Arbitrary custom tags remain rejected.
- `node tools/test_repository_review.mjs`: **15 audits, zero accessibility violations** at 1440, 1280, 390 and 320px plus offline snapshot. New tests render 46 unresolved variants grouped into 23 source declarations, exercise 20-item pagination, name/status filters, and three-tab keyboard navigation. Existing shared-trust, outage, rapid-toggle, plan/baseline export and mobile checks still pass. No page errors; no offline HTTP requests. Screenshot: `results/repository-review-browser/identities.png`.
- Same-commit external rerun: `aws-actions/configure-aws-credentials@ec8e608231b771e3614fc6ea4edadab8703331a5`, 57 inventoried files, 13 workflows, 20 declared jobs, **33 expanded job variants, 36 retained identity requests across 17 source sites**, **0 unexpanded matrices**. All 36 identities remain unresolved, no supported IAM declarations are present, and no complete path is proven. Remaining diagnostic sites: dynamic role 17, unverified local action 12, unsupported credential/session options 4. These are coverage categories, not detected vulnerabilities.
- Current external JSON/Markdown/HTML artifacts: `results/aws-actions-expanded-evidence-20260916.*`; analysis hash `de5f8b91f9cb1456cd9867a6dd30f1e39081992b84e6a2e02d8737ed8dbef419`. The pinned repository's workflows were not executed; role secret values were not retrieved. The output gives concrete evidence-needed guidance instead of an empty finding screen or fabricated role values.
- Current wheel: `dist/repository-expanded-evidence-20260916/blastradius_prototype-0.1.0-py3-none-any.whl`, SHA-256 `fc75c19f6aa7cc0eb62a1f412dad1212615678572dcba1883b580af0840c4a42`. Built offline and installed into the separate package-smoke environment. From OS temporary cwd under `-I`, the installed package passed matrix/env source resolution, intrinsic opacity, packaged shared example 5 -> 1, external identity HTML rendering and actual SARIF 2.1.0 schema validation. No source-tree fallback was used.
- Final live-browser check: the real external HTML snapshot opened with 36 identities and zero HTTP requests; the restarted app independently analyzed the exact same public commit and displayed 36 variants / 17 declarations, 33 expanded job variants, evidence-needed guidance and zero findings in Identities. The final Git status retry succeeded after an interrupted command; all changes remain uncommitted and unrelated work was preserved.
- Still unverified/unsupported: full IAM deployment, dynamic/generated matrix execution, arbitrary GitHub expressions, environment subjects, reusable/local action semantics, runtime env writes, CloudFormation intrinsic evaluation, Terraform, private remote authentication, hosted CI and manual non-Chromium/assistive-technology review. Passing these bounded tests is not production security assurance.

## Real Repository Inputs: 2026-09-16

At the user's request to test an actual repository, both public inputs below were acquired and analyzed with the current CLI. No target workflow/code was run, no Git credentials or AWS calls were used, and no implementation code was changed.

- `codezxsWIN/radius` at immutable commit `8a4f644877dc85b80e94a677e2350975ac416512`: 693 files inventoried, 2 selected/parsed, 691 outside the profile, 0 findings, 0 parser diagnostics, one oversized archive file skipped. The selected sources were the root verification workflow and a CloudFormation test fixture, so this is not a meaningful positive production-path test. Report: `results/radius-live-repository-test-20260916.{json,md}`.
- External repository `aws-actions/configure-aws-credentials` at `ec8e608231b771e3614fc6ea4edadab8703331a5`: 57 files inventoried, 13 workflow files selected/parsed, 44 files outside profile, one excluded directory, 0 matching CloudFormation files and 0 proven complete paths. **12 coverage warnings**: 5 dynamic-role ARN references and 7 unsupported matrix jobs. These are coverage diagnostics, not confirmed vulnerabilities. Report: `results/aws-actions-live-repository-test-20260916.{json,md}`.
- Pinned source inspection confirmed `cawsc-test.yml` reads `role-to-assume` from a repository secret, and `tests-integ-release.yml` declares job matrices. No secret value was requested or recovered. The parser cannot infer the missing roles/deployed permissions from those references.
- The browser's Public GitHub input analyzed the exact external commit and independently matched 13 parsed workflows, 12 diagnostics and 0 findings. It is left in that repository's Coverage view. CLI Markdown export checked the saved result's content hash. CLI baseline analysis hash: `4484591a94d603f0fb8dfada5720f0315c6f2a93d46dd365723a689bfff49c28`; its requested ref was HEAD, while the browser used the exact commit ref, so overall result hashes are not expected to match.
- Outcome: acquisition, reporting and uncertainty handling work on real inputs; the current profile **cannot complete a cloud-path assessment for this external repository**. Zero findings is not a security clearance. No provider expansion, additional parser behavior, live-cloud verification or repository publication is implied by this test.

## Investigation Workspace: 2026-09-16

- User-requested follow-up to the previous single-example screen: exact matched declaration evidence, retained-model multi-control simulation, shared-job impact, source file coverage, searchable/navigable queue and change-request exports. Baseline schema/profile is additive and the core engine is unchanged.
- Final required `tools/verify.py`: **280 passed, 1 skipped in 55.57s**; **2983/3361 executable lines, 88.753347%**. The Windows file-symlink privilege skip remains. An intermediate full run found an optional `review_context` forwarding incompatibility, repaired by retaining the old two-argument call when context is absent. Another hit a Windows reset race in the deliberately inconsistent Content-Length test; a headers-only probe now verifies early rejection without racing an unread body. Neither failure is waived in this final gate.
- New evidence regression first reproduced a mismatched role label when one job has multiple OIDC role requests. The inspector now uses the role-assumption correlation's request. Matched subject/audience, provider account and permission regions are tested and rendered as normalized facts, not raw source snippets.
- Shared example `tests/fixtures/repositories/aws-oidc-shared`: five findings, three jobs, three distinct secrets, three controls. One production-trust removal preserves all five findings through an independent trust. Both production trusts block four findings and preserve the publisher (5 -> 1); all controls block five (5 -> 0). Different input control order yields identical comparison bytes, baseline/model are unmutated, duplicate/unknown/stale controls are rejected, and per-job totals are computed from engine reach sets.
- Public-GitHub regression proves source cleanup precedes simulation. The retained context contains only graph/evidence, no temporary absolute path, and comparison makes no further transport call. Existing two-argument wrappers remain compatible.
- `node tools/test_repository_review.mjs`: **12 accessibility audits, zero violations**, including desktop 1440, laptop 1280, mobile 390, narrow mobile 320 and standalone HTML. Real-browser checks cover condition selection, finding/status/file filters, single/shared/alternate trust results, rapid double-toggle coalescing, simulated comparison outage with unknown impact and disabled export, four baseline formats and Markdown/JSON proposed change requests. No page errors and no offline HTTP requests. Scrollable source/table regions are keyboard-focusable; mobile paths use readable vertical labels.
- Current local artifacts: `results/repository-review-browser/analysis.{json,md,sarif,html}`, `change-request.{json,md}`, `checks.json`, `shared-baseline.png`, `shared-change-set.png`, `coverage.png` and `mobile.png`. Change-request exports are proposed review documents with full control and hash references, not executed patches. Browser tests inspect generated Blob content; OS save-dialog behavior is not claimed.
- Current package: wheel under `dist/repository-investigation-20260916/`, SHA-256 `1a46d48128c483945e9b01643fd1681df463316b2ec0e047543159eb0cd9dd50`, installed offline in the separate package-smoke venv. Isolated Python from temporary cwd loaded site-packages and tested both shared-removal outcomes, the proposed change request and packaged embedded-font HTML. Shared example hash: `1df8e5e9a188ab11e8177a7aabf164785cf039c2fa4623fa65f24b7d9c5daf7e`. Current SARIF also passed the locally retained 2.1.0 schema. The restarted live app was checked through Playwright: 5 baseline -> 5 after one removal -> 1 after two -> 5 after clearing.
- Offline HTML intentionally permits the saved per-finding single-statement comparison only. Live multi-control recomputation and change-request creation need the guarded local server. Deployed AWS access, manual assistive-technology testing, non-Chromium browsers and hosted CI remain unverified. Prior build/live-network checks below are historical unless repeated explicitly.

## Repository Review Delivery: 2026-09-16

- Full gate: `.venv/Scripts/python.exe -I -B tools/verify.py` ran **273 passed, 1 skipped in 59.86 seconds**. Coverage: **2854/3224 executable lines, 88.523573%**. The skip is the unchanged Windows file-symlink creation privilege test. Logs, JUnit and coverage are under results/. This is line coverage, not branch coverage, independent review or production assurance.
- Seven new failing cases first reproduced cross-job privilege pooling, ignored explicit permission/trust denies, a boundary, an unsupported additional trust condition, an environment-subject mismatch and ambiguous same-name roles. Repairs passed those regressions. Additional tests cover role account/path mismatches, managed policies/resource conditions, session restrictions and repeated use of the same trust statement. No shared-engine or frozen v0.1 semantic change was made.
- Repository view tests: deterministic self-contained HTML, embedded licensed fonts, strict inline hashes, source-text neutralization (including `</script>` payloads), SARIF review locations/code flows/fingerprints, no-proof notifications, unsafe URI rejection, saved-result hash checking and protected CLI outputs.
- Local service: 11 passing HTTP tests for loopback binding, exact-host and origin/token enforcement, stale export prevention, clearing, all formats, malformed/large/duplicate JSON, unsupported routes/preflight, excessive Content-Length, serialization of operations, invalid paths and the public acquisition dispatcher. No elevated privileges were used.
- CI: 3 passing tests run the installed trusted analyzer with the target as both cwd and PYTHONPATH, populated with hostile Python startup/shadow modules. None executed. All report hashes matched the context manifest. Tests cover no-proof/finding exit behavior, external output protection and immutable-action/permission/trusted-source workflow policy. This is local verification, not a hosted GitHub execution claim.
- `node tools/test_repository_review.mjs`: six real-Chromium audits, **zero WCAG A/AA violations**, at 1440x1080 desktop and 390x844 mobile plus offline snapshot. Interactions verify example labeling, source selection, 1 -> 0 simulation, all four export handlers, local malformed-source/no-proof and missing-directory errors, and cleared export state. No page errors; no external browser request during local analysis; zero HTTP requests from reopened offline HTML. Tests intercept the download Blob for byte inspection, not an operating-system Save dialog. Manual keyboard/screen-reader and Firefox/Safari testing remain unperformed.
- SARIF: the generated `analysis.sarif` passed `jsonschema` validation against the retrieved SARIF 2.1.0 schema. Local input tests do not require schema downloads. The schema copy is a local results artifact, not a new runtime dependency.
- Packaging: `uv build --wheel --offline --no-build-isolation` succeeded with the exact project interpreter. Installed only the wheel/runtime dependencies in `results/repository-review-package-venv`; isolated Python `-I` from the OS temporary directory loaded site-packages, analyzed the packaged two-file example, checked 1 -> 0 remediation, rendered embedded-font HTML and constructed a loopback server. No source-tree fallback, target code or signing key was used. The Windows launcher's `--help` invocation passed.
- Live public-input smoke: `analyze-github https://github.com/codezxsWIN/radius` resolved `8a4f644877dc85b80e94a677e2350975ac416512`, parsed two selected files and returned 0 findings, 0 diagnostics and the explicit no-proof conclusion. Analysis hash: `c5274de05331a91f22fc9336297310bc1a6a3e85f51328f60d4aa1bc429fc6dd`. This verified token-free GitHub acquisition, not AWS access or a security clearance.
- The bundled example artifact explicitly sets `is_example: true`; current analysis hash `65a2f17d5565e2b6fdfe911262989f3f5ee70fa1d546544b2ad2866cf363f3d9`. Reports/screenshots/audits are under `results/repository-review-browser/`; they are ignored local artifacts. Their checks do not require regenerating the user-edited legacy visual instrument.
- Unverified: deployed AWS permissions, full IAM/Terraform/provider semantics, public HTTP hosting, multiuser isolation, private remote authentication, hosted Actions execution and SARIF upload permissions. No repository/cloud change, commit or push was performed.

## Repository Recovery: 2026-09-16

The local checkout initially failed test collection with six missing-yaml errors. After the user approved reconciliation, the repository CLI commands, version-based v0.1/v0.2 validation, pinned PyYAML dependency and the existing six repository CLI tests were reinstated. PyYAML 6.0.3 installed successfully from the offline cache into blast-radius-v0.1/.venv.

- Validation and frozen conformance tests: **27 passed in 4.18 seconds**.
- CLI, repository graph, finding and Markdown-report tests: **21 passed in 30.83 seconds**.
- Full current suite: **234 passed, 1 skipped in 41.18 seconds**; a repeated run passed in 33.54 seconds. The skip is tests/test_repository_acquisition.py's file-symlink creation test because this Windows environment cannot create the link. No current coverage percentage or live-cloud verification is inferred from these runs.
- Command: `.venv/Scripts/python.exe -B -m pytest tests -q --tb=short -rs -p no:cacheprovider` from this project (executed with absolute interpreter/test paths from the workspace parent).
- The installed `blastradius analyze-repo` command produced Markdown and JSON for the checked-in aws-oidc-path fixture. One finding, one declared reachable secret, two parsed source files, zero unsupported files; a modeled trust removal changes absolute reach **1 -> 0** and marks the path broken. Source references include `.github/workflows/deploy.yml:17:27`, `infra/identity.template.json:11:13` and `infra/identity.template.json:32:17`.
- Analysis hash: `8097766d2d4bde78c7377bf9bba660c530f6df2739ef38618e1b120c4d88b530`. Example outputs are under results/repository-recovery-20260916.md and .json, outside the analyzed fixture.
- A duplicate report invocation correctly refused an existing output rather than overwriting it. Tests verify protected output, deterministic results, source immutability and explicit no-proof language. Public GitHub transport was covered by fake-transport tests; no live public GitHub smoke was repeated here.
- Reconciled code/configuration files have no editor errors. The UI, legacy demo and unrelated local changes were not regenerated or reset. No commit, push, new CI workflow or package-build success is claimed in this recovery.

## Standards Session: Executed Gate

On 2026-09-09, the final `tools/verify.py` run completed **179 tests in 39.07 seconds, all passing; 1641/1757 Python statements covered (93.397837%)**. This supersedes the intermediate 132-test standards gate and earlier prototype counts. Coverage is not branch coverage or a formal correctness proof; standalone generator/runner execution is additionally recorded below.

- Public conformance revision 2: **both Python and JavaScript passed 120/120 cases**, each run twice, over 40 fixtures and default/strict/session-theft-aware models. Suite SHA-256 `9dc834236ba7cd22c9878705447117c3cc0625ffcb23d7327b57a9456403bfc4`. Exact rational scores, pair sets, population statistics and witnesses are compared. Manifest integrity/path failures reject before invoking a tool. Detailed partial/manual boundaries are in conformance/REQUIREMENT_COVERAGE.md.
- Five reconstruction dossiers and all 30 baseline/control/model outputs were generated and replayed. Four primary-supported incident families plus one explicitly hypothetical MLflow post-acquisition scenario. Ten additional executed rank countermodels keep the target at 1/2 but change its rank from 1/10 to 10/10 under different invented background grants. No actual tenant graph or historical top-decile estimate is claimed. Beacon's 22-page final report was text-extracted and read; its primary-source uncertainty remains explicit.
-500 type-neutral synthetic summaries:500 unique exact profiles,161 unique after rounding,0 unique under the minimal projection but16 rows in groups below10. Selected epsilon ln2/k10/noisy-threshold20:200 seeded trials, mean21-cell count MAE1.014761905 including suppression;96.8% true synthetic support retained.
-500 paired tenant configurations across three regimes (1500 modeled graphs): joint NHI successes54/500 null,250/500 NHI-broader,54/500 human-broader. Protocol/run-plan hashes were saved before generation; all output hashes and selected graph/outcome reproductions pass tests. Synthetic results cannot accept/reject a real-population hypothesis.
- Paper Related Work measured1227 whitespace-delimited words. Governance/specification/paper/research documents passed editor diagnostics. No neutral home, elected steering group, privacy review, real intake, live red-team run, DOI or paper submission exists.
- The single user-edited demo HTML was preserved, not regenerated in this standards session. Historical browser/scale results below remain prior evidence, not new measurements.

### Final Package and Stretch Gates

- JavaScript's dependency-free reference passed `node reference-js/verify.mjs` plus the external runner. Parser checks cover escaped duplicate keys, lossy decimal rejection, large integer preservation and exact canonical-number bytes. This is not independent authorship or a general JSON Schema implementation.
- Kubernetes offline read-only profile: **17 tests passed**, with observer reach 4/6, worker 3/6, both zero-step reach 3/6; sensitivity radii 5/8 and 3/8. Namespace scope, Secret list/watch value access, explicit stored credentials and unsupported-input rejection are tested. No kubeconfig or cluster call is made.
- `uv build --offline` succeeded. `tools/smoke_wheel.py` installed the wheel and only declared cached dependencies in a fresh temporary venv, removed source-path influence and ran isolated Python `-I` outside the source tree: **120/120 public cases and Kubernetes normalization passed**. Tested wheel SHA-256: `1c88ba492753fe5f292fb019e0d0c21413efafd08a38ba0fde38c1d0107c6f68`; any subsequent documentation rebuild receives a new archive hash and is separately audited.
- `tools/audit_release.py` passed 42 checks covering 38 required artifacts, reports, protocol/output hashes and eight theorems. Source-archive inspection found all required artifacts and no keys, virtual environments, live exports or source PDFs. Final checkpoint ZIP bytes are audited against the current source separately.
- Core specification/report/research artifacts were committed locally as `51cb0d1`. Final extension/documentation commits are recorded in the final handover; no remote, public push or GitHub-hosted CI execution occurred.
- Preserved demo SHA-256: `3a73122a30a84d31732e33454ede34b65767f4ed2e1af4f18ba2fb979f869408`. It remains unmodified and is included in the archive and final commit without regeneration. Text line endings are pinned to LF, except the two prospectively hashed protocol documents, whose original CRLF bytes are preserved with `-text`; their pre-data commitments are not rewritten.

## Historical Prototype Session

The previous release recorded **108 tests in16.56 seconds and94.8148% Python line coverage (1152/1215 statements)**. The entries below retain that session's checkpoint history. Only executed measurements are claimed.

## Foundation Checkpoint: Executed

- Command: `../.venv/Scripts/python.exe -B -m pytest tests -q` (also run from parent with the project test path).
- 35 tests passed in 1.06 seconds: 13 hand-computed fixtures, five property tests, schema validation, ground-truth parity, signed-byte determinism and tamper rejection.
- `tools/materialize.py` generated schema/example.json, its independently computed expected file and all thirteen conformance JSON documents.
- A failing permutation test exposed unsorted action arrays in report path evidence; normalizing at the analysis boundary fixed it. The complete suite was rerun successfully.
- Later measured coverage and benchmark records supersede this initial checkpoint below.

## Demo Checkpoint: Executed

- `pytest tests --cov=blastradius --cov-report=term-missing --cov-report=json:results/coverage.json -q`: 41 tests passed in 16.63 seconds; terminal rounded coverage 88% (759 statements, 93 missed; exact percentage is in coverage.json).
- Required workload: 10000 principals, 2000 resources, 32119 edges, 5999 resource-action pairs. Generation 1.408 s; schema validation 36.612 s; analysis 12.983 s; serialization 0.623 s; total 51.626 s. No recommendation search or cloud collection is included. Sparse-tail formula assertions passed.
- Classic workload: 48000 pairs; finance 1240/48000, CI/CD 9600/48000, legacy 31200/48000, Copilot 14400/48000, all asserted. Total 34.127 s. Weighted values derive from the documented constructed graph, not the charter's absent weight profile.
- Dashboard: actual 80-credential result; max 51.2605042%, p95 34.4537815%, Gini 0.0263474682, 100% above 25%. All 90 eligible allow-binding removals evaluated; best `shared-2` lowers p95 to 27.7310924%.
- Browser checks executed at 1440x1000 and 390x844: all 80 rows, 10 histogram bins, search, 20-row agent filter, numeric sort, credential detail and second what-if values. Mobile document width <= viewport. Screenshots inspected.
- Browser tests found and fixed a missing filter assignment and a mobile grid min-width overflow. HTML was regenerated from the same signed result after each fix.

## Connector Checkpoint: Executed

- Integrated suite: 59 tests passed in 23.52 seconds; 1054 executable Python statements, 132 missed (87.48% line coverage). Browser JavaScript is validated by separate interaction assertions, not included in Python line coverage.
- Fifteen initial connector/transport cases and additional artifact tests use only local synthetic exports and fake responses. Basic import: 1/4 and 1/4. Rich import default: 0/6, 2/6, 3/6; permissive: 2/6, 3/6, 4/6.
- GET-only transport checks cover disabled-by-default behavior, method/body restrictions, URL allowlists, redirect refusal, pagination, hostile nextLinks and bounded Retry-After handling. No real tenant call or authentication was performed.
- The installed `collect-entra-azure --exports connectors/entra_azure/fixtures/rich --out results/connector-tenant.json` command ran successfully.
- Microsoft Learn references and explicitly unverified endpoint/permission details are in connectors/entra_azure/manifest.json and PERMISSIONS.md. Corrected Key Vault list API version: 2024-11-01.

## Stretch Benchmark: Executed

Command: `.venv/Scripts/python.exe -B tools/benchmark.py --principals 50000 --resources 2000 --seed 7 --out results/benchmark-50000.json`.

50000 principal/credential records; 2000 resources; 152019 graph nodes; 152119 edges; 5999 resource-action pairs. Generation 9.654 s; schema validation 158.499 s; analysis 45.331 s; serialization 1.902 s; total wall time **215.385 seconds**, below the five-minute target. The serialized full result is 113419685 bytes; only the timing summary is retained for this stretch to keep checkpoint artifacts small. Top ten paths were included; exhaustive intervention search and live collection were excluded. All ordinary principal IDs >=80 matched their independently known sparse-tail counts. The spotlight topology is covered by smaller oracle/conformance fixtures.

Environment for both scale measurements: CPython 3.12.10, Windows 11 build 26200, Intel64 Family 6 Model 170 Stepping 4, 22 logical CPUs. Timing uses perf_counter; no artificial waits or extrapolated estimates. These are not worst-case graph or live-tenant guarantees.

## Hardened Prototype Gate: Executed

`.venv/Scripts/python.exe -B tools/verify.py` ran 87 pytest tests successfully in 31.22 seconds. Coverage: 1014 of 1074 executable statements, **94.4134% Python line coverage**. The entry-point and some subprocess-only lines are not covered by the in-process coverage collector; subprocess workflows were nevertheless actually executed. Branch coverage and JavaScript percentage coverage are not claimed. Saved machine records: results/coverage.json, results/junit.xml and results/pytest-output.txt.

The suite includes 13 small hand-computed fixtures, four classic cardinality checks, two saved connector bundles and six property test functions covering the five requested property categories, including independent-source and same-principal composability. Additional tests check malformed/secret-bearing inputs, unknown semantics, signed tampering, actor-local denies, what-if addition scope, failure retry/exhaustion and safe HTML embedding.

`tools/build_demo.py` re-executed all public demo commands after hardening, verified the HMAC and compared repeated signed result bytes. Manifest hash remained `5e840d2e4fe81cfe54bcbb03a947915957cb6dba01023e63166c96da6331d6c6`; source snapshot hash remained `eba49065f6a70a90c43c6fa7bbe2f452bb60d2dcbed2bf216708a97fe13d584d`.

`uv build --offline --directory . --out-dir dist --quiet` produced a wheel and source archive using cached setuptools 80.9.0. No publication or registry push was attempted. Package-install verification is recorded at the final handover checkpoint.

## Final Integrated Code Gate: Executed

After the bounded AWS adapter, privacy preview and final actor-scoped membership regression, `tools/verify.py` ran **103 tests in 29.11 seconds, all passing**. Coverage: 1151 of 1215 executable statements, **94.7325% Python line coverage**, rounded to 95% by the terminal. The saved coverage/JUnit/output records supersede earlier counts in this checkpoint history.

The AWS fixture asserts user 4/4 and role 2/4, with user zero-escalation reach 2/4. Tests remove either identity permission or trust, reject conditions/boundaries/SCP ambiguity and credential material, and compare decoded versus URL-encoded policy normalization. The structural dry run is socket-blocked in tests and explicitly rejects a real-data marker or non-dry-run invocation.

The final engine regression proves a device-gated group membership cannot authorize a group-derived credential-acquisition edge; both the production queue and independent whole-edge oracle agree. Static membership for deny applicability remains separate, so a stricter attacker model cannot remove a deny to invent access.

## Release Verification: Executed

Final full suite after explicit deny what-if support: **104 passed in 28.97 seconds**. Coverage JSON reports **1149 covered lines / 1213 statements = 94.72382522671063%**; 64 missed lines. The earlier checkpoint counts above are historical. No known failing tests or editor diagnostics remain.

Clean-wheel smoke: installed dist/blastradius_prototype-0.1.0-py3-none-any.whl with only declared runtime dependencies in a fresh temporary venv, removed PYTHONPATH and invoked isolated `python -I -B -m blastradius` from a separate temporary directory. Synth, schema validation, signed analysis, manifest verification, HTML report assets and saved AWS offline ingestion all passed. The wheel includes schema and dashboard assets; the source archive includes hand fixtures, connector manifests and permissions. No signing keys, local environments or real exports were packaged.

Final tools/build_demo.py execution preserved the identical manifest hash and verified every public demo artifact. Desktop (1440x1000) and phone (390x844) checks include the fifth what-if row, exact after-p95 values, no page-width overflow and no external script/style/image dependency. JavaScript was disabled through the browser protocol and all 80 static credential rows remained visible; JavaScript was then restored and the page reloaded.

Benchmark times are the executed session measurements recorded above, not extrapolations or timing claims for arbitrary graphs. The later input/deny/HTML hardening did not change demo values; it was validated by the final suite and full public demo rebuild. Full independent oracle analysis is intentionally limited to small fixtures; the large benchmark uses independently known sparse-tail counts plus small spotlight conformance.

## Final Save Gate: Executed

The release audit added controlled rejection of malformed signature objects and nonobject manifest files; four regression cases were added. The exact tools/verify.py command above then completed with **108 passed in 16.56 s, 1152 covered / 1215 statements, 63 missed = 94.81481481481481%**. The installed verify-manifest command verified the unchanged demo manifest afterward. All editor diagnostics are clean.

OneDrive checkpoint ZIP integrity, required members, SHA-256 receipt, loose-file equality and exclusion of keys/environments were programmatically checked at the release checkpoint. Final copies retain these same checks. Cloud synchronization is not independently verified. No GitHub publication or workflow execution, Azure resource operation, AWS account operation, or real tenant access occurred.
