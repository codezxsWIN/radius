# BR-015: two-actions

r0/read and r0/write are distinct; alpha reaches 2/4, sensitivity 0.4/2 and action 4/8.

Expected outputs are frozen from the independent whole-edge oracle, with pairwise-difference Gini rather than the production rank formula.

## default

- credential-alpha: pairs=[['r0', 'read'], ['r0', 'write']]; absolute=2; universe=4; canonical=1/2; sensitivity=1/5; action=1/2; k=1 bounded=1/2.

## strict

- credential-alpha: pairs=[['r0', 'read'], ['r0', 'write']]; absolute=2; universe=4; canonical=1/2; sensitivity=1/5; action=1/2; k=1 bounded=1/2.

## session-theft-aware

- credential-alpha: pairs=[['r0', 'read'], ['r0', 'write']]; absolute=2; universe=4; canonical=1/2; sensitivity=1/5; action=1/2; k=1 bounded=1/2.
