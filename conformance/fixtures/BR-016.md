# BR-016: fixed-universe

Add an ungranted declared resource/action: alpha remains one pair but denominator becomes five; absolute reach prevents hiding this dilution.

Expected outputs are frozen from the independent whole-edge oracle, with pairwise-difference Gini rather than the production rank formula.

## default

- credential-alpha: pairs=[['r0', 'read']]; absolute=1; universe=5; canonical=1/5; sensitivity=1/15; action=1/9; k=1 bounded=1/5.

## strict

- credential-alpha: pairs=[['r0', 'read']]; absolute=1; universe=5; canonical=1/5; sensitivity=1/15; action=1/9; k=1 bounded=1/5.

## session-theft-aware

- credential-alpha: pairs=[['r0', 'read']]; absolute=1; universe=5; canonical=1/5; sensitivity=1/15; action=1/9; k=1 bounded=1/5.
